Phase 9: RPF Archive Mounting + VFS Implementation

- Implemented RpfVirtualFileSystem in src/vfs_rpf.{h,cpp}:
  - Virtual t:\ drive mapping to extracted cache (E:/mcla pc/mcla extracted cache)
  - File operations: OpenFile, ReadFile, CloseFile, SeekFile, ListDirectory
  - Path normalization: t:\mc4\... -> mc4/...
  - Thread-safe with mutex protection
  - File index built from extracted cache at startup

- Integrated with NtCreateFile hook in patches.cpp:
  - Detects t:\ paths and checks VFS for existence
  - Logs VFS hits for diagnostics
  - Graceful fallback for test_*.loc files (returns 0xC0000034)

- Integrated VFS initialization in mcla_ApplyPatches()

- All 7 validators CLEAN, build clean (14/14 ninja steps)

Phase 9 Progress: RPF Archive Mounting complete. Next: Art Asset Pipeline (.dff/.txd parsing) and Boot Validation.