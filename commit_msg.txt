Phase 9: Art Asset Pipeline - RAGE Format Parser

- Created src/renderer/rage_asset_pipeline.{h,cpp}:
  - CsrContainer: Parses RAGE CSR (0x525343) container format
  - Parses CSR header (version=5, magic="CSR", entries)
  - CsrEntry parsing: hash, offset, size, flags, type, unk
  - TxdParser: Placeholder for .txd texture dictionary parsing
  - DffParser: Placeholder for .dff model parsing
  - RageAssetManager: Singleton asset manager
    - LoadTextureDictionary()/LoadModel() via VFS
    - GetTextureData()/GetModelGeometry() for native renderer
    - GetOrCreateTexture()/GetOrCreateModel() for D3D12 resources
  - CSR header parsing: version=5, magic="CSR", big-endian fields
  - Entry parsing: hash, offset, size, flags, type, unk
  - VFS integration: LoadCsrContainer() loads from virtual t:\ drive

- Integrated with VFS (src/vfs_rpf.{h,cpp}) for RPF archive mounting
- VFS indexes extracted cache at E:/mcla pc/mcla extracted cache
- Path normalization: t:\mc4\... -> mc4/...

- Integrated with NtCreateFile hook in patches.cpp:
  - Detects t:\ paths and checks VFS for existence
  - Logs VFS hits for diagnostics
  - Graceful fallback for test_*.loc files (returns 0xC0000034)

- Integrated VFS initialization in mcla_ApplyPatches()

- All 7 validators CLEAN, build clean (14/14 ninja steps)

Next: .dff/.txd format parsing, .stream loading, boot validation to reach hasGrcFvf=1