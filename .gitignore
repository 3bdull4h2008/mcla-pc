# Build output
/build/
/build_pathb/
/pathb/
/build-smoke/
/build-standalone/

# Derived / regenerable
# NOTE: /docs/ was ignored here until 2026-09-20. It is NOT regenerable — it is the
# cross-session handoff trail, and it has already been lost once to `git clean -xdf`.
# Every doc had to be `git add -f`'d individually. Do not re-ignore it.
/game_data/
/mcla extracted cache/
/.research/
/_archive/

# Binaries / downloaded
/SDL3.zip
/SDL3_extract/
# NOTE: /tools/ was ignored here until 2026-09-20 (it once held downloaded binaries).
# It now holds source: the phase-gate validators, the RE scripts that back the
# raw-byte rule, and tools/xenonrecomp_local.patch — the only preserved copy of the
# JTS-TAILCALL recompiler patch. Ignoring it hid 16 files from `git status` entirely.
# Do not re-ignore it; ignore specific binary subpaths instead if one ever appears.
rexglue-sdk/
*.exe
*.dll

# Logs / dumps / crash artifacts
*.log
*.dmp
*.tracy
crash_dump.txt
crash_log_early.txt
boot_stderr*.log
boot_stdout*.log
boot_test_capture.png
/logs/

# Build artifacts
*.obj
*.d
*.pid
*.err
ninja_out.txt
ninja_err.txt
build_s74.txt
build_log.txt
build_err.txt
build_out.txt
cache/

# Generated / regenerable source
generated/
generated/default/mcla_recomp.*.cpp
generated/default/func_lookup.clixml

# SDL3 binaries
third_party/SDL3/SDL3-3.2.6/lib/
third_party/SDL3/lib/

# Archives
*.tar.gz
*.tgz
*.zip
*.gz

# AI tooling (local only)
.opencode/
.clinerules/
.mcp.json

# Local tooling / caches
.cache/
.freebuff/
.vscode/

# Local config
/opencode.json
/config/opencode.json
/config/config.json

# OS / editor
Thumbs.db
.DS_Store
