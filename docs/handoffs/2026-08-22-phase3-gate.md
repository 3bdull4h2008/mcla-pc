# Handoff — MCLA Native PC (E:\mcla pc) — 2026-08-22

**Verified:** HEAD = `a750e36` (kernel: log MmAllocatePhysicalMemoryEx failures) ✔ matches.
Chain confirmed via git log: `39ebebf` → `0c35ac4` → `a114b34` → `9cf5a4f` → `a750e36`.
Persisted by memory-steward subagent; second-brain MCP unavailable at persist time.

## MILESTONE [2026-08-22]: PHASE 3 GATE PASSED
Commit `39ebebf` — boot survives main loop, VSync present chain firing 60fps,
thousands of frames. Game boots, loads world (all 4 xarchive_*.rpf served as
REAL bytes from dump root), runs main loop, zero crashes.

## WHAT KILLED THE WALL
1. Raw PPC_FUNC fakes at top of imports.cpp shadowed typed hooks.
2. Game queries FileInformationClass 34 (FileNetworkOpenInformation) and gates
   on EndOfFile == real size; zeros meant 'empty file' → 'Cannot load archive'
   fatal. Fixed with true sizes + archive attribute.
   *(verified: imports.cpp:57–81 — "packfile loader gates on EndOfFile being
   the REAL archive size")*
3. VdSync interrupt r3/r4 type fix (`0c35ac4`: type in r3, 0=graphics,
   1=vsync event — not userData).
4. NtReadFile OOB clamp: **worktree MapVirtual version is correct**
   *(verified: imports.cpp:868–888 — MapVirtual → Translate → window-size
   clamp)*. Committed-in-39ebebf version truncated host ptr; superseded by
   `9cf5a4f`.

## HEALTH CHECK (import tracer, `a114b34`)
13 guest threads alive; events/semaphores/critical-sections operational;
IO workers healthy-idle; main thread parked silently — sleeps WITHOUT touching
hooked imports. Finite-timeout Event/Semaphore hot-spin fixed (`9cf5a4f`).
MmAllocatePhysicalMemoryEx exhaustion ruled out (`a750e36`).

## OPEN QUESTION #1 (next session primary target)
What does the main thread wait for before rendering? Probe design committed
(main-thread-only waits w/ timeouts + LRs from KeDelayExecutionThread /
KeWaitForSingleObject). Candidates: unsignaled event/semaphore dependency,
streaming completion, input init, message/port wait.

## SECURITY STATE
- Both prior HIGHs FIXED + audit-verified: NtReadFile clamp sound (worktree
  form); query stubs bounds-checked.
- OPEN (MED): identity-handle ABA/generation counter — do before Phase 4 adds
  handle types.

## SHIP-BLOCKERS BEFORE PHASE 4 TAG
1. Strip function.h import-trace probe + its includes
   *(verified present: function.h:282–287, TEMPORARY probe, 4000-cap stderr
   "[import-trace] lr=%08X fn=%s", committed in `a114b34`)*.
2. Demote per-frame VSync INFO logs.
3. Manual ctx.rN query wrappers → consolidate onto existing dead typed impls
   *(verified: imports.cpp:825 = NtQueryVolumeInformationFile,
   imports.cpp:846 = NtQueryDirectoryFile)*.
4. Class-34 fallback statuses must be INVALID_INFO_CLASS /
   INFO_LENGTH_MISMATCH per xenia — not SUCCESS+zeros.

## PHASE 4 RECON (research-scout, high confidence)
Xenia CP model: WPTR doorbell MMIO trap @ phys 0x7FC80000 reg 0x01C5 → CP
drains ring put≠read wrap-aware → BE-store rptr to writeback addr. Minimal
packet set: SET_CONSTANT / IM_LOAD(_IMM) / DRAW_INDX_2 / XE_SWAP (0x64,
Xenia-invented). Frontbuffer arrives pre-resolved from EDRAM → no EDRAM
emulation needed to present. UnleashedRecomp chose D3D9-export-hook boundary
instead of CP emulation.
Gap table: ring storage PARTIAL · doorbell MISSING · CP worker PARTIAL
(guest-fn drain on VSync thread today = remove per manual-seeding plan) ·
rptr writeback MISSING · present queue PARTIAL (inline on guest thread).

## RULES REMINDER
One hook owner/address · hooks capture-only · wire ONE import at a time ·
no invented data · legacy mode stays default until gates pass.
